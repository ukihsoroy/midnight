package org.ko.prototype.generator.builder;

import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Scanner {

	private static final Logger log = LoggerFactory.getLogger(Scanner.class);
	
	@Test public void scan() throws Exception {
//		File file = new File(Scanner.class.getClassLoader().getResource(".").toURI());
//		File root = file.getParentFile().getParentFile().getParentFile();
//		
//		ScannerApp.execute(root.getAbsolutePath());
	}
}
